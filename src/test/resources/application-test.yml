# Logging levels
logging.level:
  root: INFO
  ${project.group}: DEBUG

spring.boot.admin.client.enabled: false

spring.redis:
  port: '\${embedded.redis.port}'
  password: '\${embedded.redis.password}'

# Application
location:
  latitude: 52.23
  longitude: 21.01
provider.path: 'http://localhost:\${wiremock.server.port}/sunrise-sunset'
scheduler:
  init-on-start: false
  cron: '-'
